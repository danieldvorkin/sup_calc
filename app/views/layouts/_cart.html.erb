<div class="cart-text">
  <%= link_to "#{current_order.present? ? current_order.order_items.size : "0"} Items in Cart ( #{current_order.present? ? (number_to_currency current_order.subtotal) : "$0"} )", cart_path %>
</div>
